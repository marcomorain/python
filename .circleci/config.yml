version: 2.1
jobs:
  build:
    machine:
      image: circleci/classic:201808-01
    steps:
      - checkout
      - run: bash -version # Show bash version
      - run: echo $0       # Show current shell
